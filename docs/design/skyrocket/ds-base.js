// SkyRocket is a self-contained template with its own aesthetic.
// It deliberately loads neither the Industry stylesheet nor the bundle;
// fonts and resets live in the DC's <helmet>.
(() => {})();
